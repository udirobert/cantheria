"""Cantheria — where the canaries are kept.

SIFT pipeline: embed a repo → hunt its highest-blast-radius code with an
open model → validate each hypothesis through a crash oracle and reproduction
runs → triage with a reranker → emit maintainer-ready reports.
"""

__version__ = "0.1.0"
